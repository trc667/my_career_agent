# 生产环境 API 地址（部署时复制为 .env.production 并填写实际地址）
# 留空 = 走同域（Netlify _redirects 代理 /api）；填域名 = 前端直连后端
VITE_API_BASE_URL=

# 流式接口专用地址（可选，推荐配置）
# Netlify 的 _redirects 代理会缓冲 SSE 流式响应，导致内容晚到/被截断丢失，
# 打字机效果失效。生产环境建议让流式请求直连后端（如 cpolar 隧道）绕过代理：
# VITE_STREAM_BASE_URL=https://你的后端域名或隧道地址
VITE_STREAM_BASE_URL=https://6dd3eff9.r11.cpolar.top
